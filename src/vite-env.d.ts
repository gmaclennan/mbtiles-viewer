/// <reference types="vite/client" />

/** Replaced at build/dev-server-start time via vite's `define`. Use this to
 *  identify which build a remote device is running. */
declare const __BUILD_VERSION__: string;

